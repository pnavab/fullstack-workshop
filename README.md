# Fullstack Workshop Demo code

### To Run
- Run `npm install` to install all dependencies
- Run `npm start` to load the frontend
- Run `npm run server`to run the Express server that connects to the JSON mock database
